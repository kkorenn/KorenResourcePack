# Unity Projects

- `current/` builds AssetBundles for the current ADOFAI target with Unity 6000.3.10f1.

The project keeps Unity's expected root layout (`Assets/`, `Packages/`, `ProjectSettings/`). Do not move files inside `Assets/` without their `.meta` files.
