// Main key layout style enum / 主按键布局样式枚举

namespace JipperKeyViewer.KeyViewer
{
    /// <summary>
    /// Main key layout styles supported by the mod / Mod 支持的主按键布局样式
    /// </summary>
    public enum KeyviewerStyle
    {
        /// <summary>12-key layout (8+4) / 12键布局（8+4）</summary>
        Key12,
        /// <summary>16-key layout (8+8) / 16键布局（8+8）</summary>
        Key16,
        /// <summary>20-key layout (8+8+4) / 20键布局（8+8+4）</summary>
        Key20,
        /// <summary>10-key layout (8+2) / 10键布局（8+2）</summary>
        Key10,
        /// <summary>8-key layout (single row) / 8键布局（单排）</summary>
        Key8,
        /// <summary>14-key layout (8+6) / 14键布局（8+6）</summary>
        Key14
    }
}
