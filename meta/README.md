# Metadata

This directory is the source of truth for the Unity Mod Manager manifest. Build and release packaging copies [`Info.json`](Info.json) into the shipped mod as `Info.json`, paired with `KorenResourcePack.dll`.

Keep the manifest's `AssemblyName` aligned with the DLL produced by the build.
